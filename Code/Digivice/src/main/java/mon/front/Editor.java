package mon.front;

import javax.swing.*;

public class Editor extends JFrame {
    public Editor() {
        JFrame frame = new JFrame();
        frame.setSize(350, 200);
        frame.setVisible(true);
    }
}
